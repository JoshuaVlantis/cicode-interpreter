{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cicode-interpreter",
            "request": "launch",
            "name": "Debug CiCode",
            "program": "${file}"
        }
    ]
}
